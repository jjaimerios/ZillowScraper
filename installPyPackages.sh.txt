#!/bin/bash

echo "Starting Python Installs"

# sudo apt-get install pip
# Prompts for pw - so run this manually

pip install beautifulsoup4
pip install selenium
pip install regex

# Parser Library for BeautifulSoup
pip install lxml

# Property files module
pip install ConfigParser

pip install pymongo

pip install pandas
pip install matplotlib
pip install sqlalchemy

exit 0
